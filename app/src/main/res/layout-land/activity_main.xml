<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity"
    android:background="#FBFBFB">

    <androidx.appcompat.widget.Toolbar
        android:id="@+id/toolbar"
        android:layout_width="match_parent"
        android:layout_height="@dimen/toolbar_height"
        android:background="@drawable/rounded_toolbar_background"
        android:theme="@style/ToolbarTextStyle"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent">

        <TextView
            android:id="@+id/toolbar_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:gravity="center"
            android:text="@string/task1"
            android:textColor="@android:color/white"
            android:textSize="@dimen/toolbar_title_size_g"
            android:theme="@style/ToolbarTextStyle" />
    </androidx.appcompat.widget.Toolbar>

    <TextView
        android:id="@+id/outputResult"
        android:layout_width="@dimen/input_width_g"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/output_margin_top_g"
        android:text=""
        android:textColor="@color/orange"
        android:inputType="textMultiLine"
        android:maxLines="@dimen/lines"
        android:scrollbars="vertical"
        app:layout_constraintEnd_toEndOf="@+id/filterText"
        app:layout_constraintStart_toStartOf="@+id/filterText"
        app:layout_constraintTop_toBottomOf="@+id/filterText" />

    <com.google.android.material.textfield.TextInputLayout
        android:id="@+id/inputText"
        android:layout_width="@dimen/input_width_g"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/input_margin_top_g"
        android:hint="@string/enter_text"
        android:maxLines="@dimen/lines"
        android:scrollbars="vertical"
        app:boxBackgroundMode="outline"
        app:boxCornerRadiusBottomEnd="@dimen/button_margin_top_g"
        app:boxCornerRadiusBottomStart="@dimen/cornerRadius"
        app:boxCornerRadiusTopEnd="@dimen/cornerRadius"
        app:boxCornerRadiusTopStart="@dimen/cornerRadius"
        app:boxStrokeColor="@color/black"
        app:hintTextAppearance="@style/HintTextStyle"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.497"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent">

        <com.google.android.material.textfield.TextInputEditText
            android:id="@+id/inputTextLayout"
            android:layout_width="match_parent"
            android:layout_height="65dp"
            android:ems="@dimen/ems"
            android:gravity="start|top"
            android:hint=""
            android:inputType="textMultiLine"
            android:scrollbars="vertical"
            android:textSize="@dimen/input_text_size_g" />
    </com.google.android.material.textfield.TextInputLayout>

    <com.google.android.material.textfield.TextInputLayout
        android:id="@+id/filterText"
        android:layout_width="@dimen/filter_width_g"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/filter_margin_top_g"
        android:hint="@string/enter_symbols"
        android:maxLines="@dimen/lines"
        android:scrollbars="vertical"
        app:boxBackgroundMode="outline"
        app:boxCornerRadiusBottomEnd="@dimen/cornerRadius"
        app:boxCornerRadiusBottomStart="@dimen/cornerRadius"
        app:boxCornerRadiusTopEnd="@dimen/cornerRadius"
        app:boxCornerRadiusTopStart="@dimen/cornerRadius"
        app:boxStrokeColor="@color/black"
        app:hintTextAppearance="@style/HintTextStyle"
        app:layout_constraintEnd_toEndOf="@+id/inputText"
        app:layout_constraintHorizontal_bias="0.497"
        app:layout_constraintStart_toStartOf="@+id/inputText"
        app:layout_constraintTop_toBottomOf="@+id/inputText">
        <com.google.android.material.textfield.TextInputEditText
            android:id="@+id/filterTextLayout"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:ems="@dimen/ems"
            android:gravity="start|top"
            android:hint=""
            android:inputType="textMultiLine"
            android:scrollbars="vertical"
            android:textSize="@dimen/input_text_size_g" />
    </com.google.android.material.textfield.TextInputLayout>

    <Button
        android:id="@+id/ConvertButton"
        android:layout_width="@dimen/button_width_g"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/button_margin_top_g"
        android:background="@drawable/rounded_button"
        android:text="@string/convert"
        app:layout_constraintEnd_toEndOf="@+id/filterText"
        app:layout_constraintHorizontal_bias="0.0"
        app:layout_constraintStart_toStartOf="@+id/filterText"
        app:layout_constraintTop_toBottomOf="@+id/outputResult" />
</androidx.constraintlayout.widget.ConstraintLayout>
